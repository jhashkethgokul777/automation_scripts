# Python Automation Scripts

This repository contains simple and useful automation scripts created while learning Python automation.  
Each script automates a common task such as renaming files, checking folders, moving files, etc.

---

## 📂 Folder Structure

### file_operations/
Scripts that modify or rename files:

- `change_file_ext.py` – Change extension of all files in a folder  
- `file_mover.py` – Move files based on type/extension  
- `rename_num_frmt.py` – Rename files into numeric sequence  
- `space_underscore.py` – Replace spaces with underscores  

### folder_operations/
Scripts that check, list, or organize folders:

- `Check_folder.py` – Check if folder exists  
- `list_files.py` – List all files in directory  
- `fixed_prefix.py` – Add prefix to filenames  

---


Install dependencies:


